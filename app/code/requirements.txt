# Pinned to match the conda env ("ML") the model was trained/pickled in.
# numpy in particular MUST match: numpy 2.x and 1.x use different internal
# pickle formats for RandomState/BitGenerator objects (the random_state=SEED
# baked into the fitted estimators), so a mismatch fails at joblib.load with
# "... is not a known BitGenerator module" rather than a normal version error.
dash==2.17.1
dash-bootstrap-components==1.6.0
pandas==2.2.3
numpy==2.0.2
scikit-learn==1.6.0
joblib==1.4.2
gunicorn==22.0.0